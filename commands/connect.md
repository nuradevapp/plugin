---
description: Get a Nura Dev pairing code to connect your phone
---

The user wants to connect their phone to this Claude Code session via Nura Dev. Call the `request_pairing_code` tool from the nuradev MCP server. A system channel event will arrive shortly with the pairing code drawn in a box — display that event content verbatim (preserve all box-drawing characters and line breaks) so the user can read the 6-character code and enter it in the Nura Dev iOS app.
